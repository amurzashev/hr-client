import React from 'react';
import styled from '@emotion/styled';

const Card  = styled.div`
  width: 240px;
  height: 120px;
  border-radius: 3px;
  background: red;
  cursor: pointer;
`;

const Preview = () => {
  return <Card />
};

export default Preview;
